node_modules
dist
.DS_Store
.env

# Ignore the output video from Git but not videos you import into src/.
out

# Staged, white-keyed avatar art copied in at render time (source lives in
# assets/toolverse/).
public/toolverse

# Staged ranking media (Pexels backgrounds + tool favicons), fetched fresh
# each render by render_video.py _stage_ranking_media().
public/ranking

# Staged insight keyword-image cards (Pexels photos), fetched fresh each
# render by render_video.py _stage_insight_images().
public/insight
